-- ============================================
-- Eliminar configuración de datos del condominio
-- (queda hardcodeado en el código)
-- ============================================

DELETE FROM config WHERE key = 'condominio';
